export async function getWeatherInfo() {
    try{
        //Make API call to get the weather info 
        /*
        const response = await fetch('/api/');
        return await response.json();
        */
    }catch(error)
    {
        return [];
    }
}
export async function saveWeatherInfo() {
    try{
        //Make API call to save the weather info 
        /*
        const response = await fetch('/api/');
        return await response.json();
        */
    }catch(error)
    {
        return [];
    }
}